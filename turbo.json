{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["lib/**/*.ts"],
      "outputs": ["lib/**/*.js"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
